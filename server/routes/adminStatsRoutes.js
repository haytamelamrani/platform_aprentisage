/* const express = require('express');
const router = express.Router();

const { getProgressByCourse } = require('../controllers/progressController');
const { getRatingsAverage } = require('../controllers/courseController');
const { getTimeSpentByUser } = require('../controllers/userStatsController'); // ou autre fichier selon ton projet

router.get('/progress', getProgressByCourse);
router.get('/ratings', getRatingsAverage);
router.get('/time-spent', getTimeSpentByUser);

module.exports = router;
 */